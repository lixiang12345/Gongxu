# Preserve brownfield architecture by default

For an existing repository, Gongxu records current architecture separately from
the target and defaults the target to `preserve-current`. A migration or repair
becomes a confirmed or proposed decision only after the owner asks for it;
inferred target architecture never becomes a blocking rule by itself.
